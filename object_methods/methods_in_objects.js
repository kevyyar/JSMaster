const math = {
    add (x, y) {
       return x + y;
    },
    multiply(x, y) {
        return x * y;
    },
    divide(x, y) {
        return x / y;
    },
    subtraction(x, y) {
        return x - y;
    }
}

console.log(math.divide(12, 6))