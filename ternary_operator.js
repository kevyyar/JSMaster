// condition ? expIfTrue : expIfFalse

let num = 13;

num === 7 ? "Lucky number!" : "That's not a lucky number";

let status = "offline";
let color = status === "offline" ? "red" : "green";

let age = 18;

age === 18 ? "You're allowed to enter" : "You're not old enough to enter";
